alembic==1.13.1
bcrypt==4.1.2
blinker==1.7.0
click==8.1.7
colorama==0.4.6
dnspython==2.5.0
email-validator==2.1.0.post1
Flask==3.0.2
Flask-Bcrypt==1.0.1
Flask-Login==0.6.3
Flask-Migrate==4.0.5
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
greenlet==3.0.3
idna==3.6
itsdangerous==2.1.2
Jinja2==3.1.3
Mako==1.3.2
MarkupSafe==2.1.5
SQLAlchemy==2.0.25
typing_extensions==4.9.0
Werkzeug==3.0.1
WTForms==3.1.2



alembic==1.13.1: Alembic est un outil de migration de base de données pour SQLAlchemy.

bcrypt==4.1.2: Bcrypt est une bibliothèque de hachage de mots de passe sécurisée.

blinker==1.7.0: Blinker est une petite bibliothèque pour permettre la gestion des événements en Python.

click==8.1.7: Click est une bibliothèque pour créer des interfaces en ligne de commande dans Python.

colorama==0.4.6: Colorama est une bibliothèque Python pour faciliter la coloration de texte dans la console.

dnspython==2.5.0: dnspython est une bibliothèque pour effectuer des requêtes DNS avec Python.

email-validator==2.1.0.post1: Email-validator est une bibliothèque pour valider les adresses e-mail en Python.

Flask==3.0.2: Flask est un framework web léger pour Python.

Flask-Bcrypt==1.0.1: Flask-Bcrypt est une extension Flask pour l'intégration de la fonction de hachage de mots de passe bcrypt.

Flask-Login==0.6.3: Flask-Login est une extension Flask qui gère l'authentification des utilisateurs.

Flask-Migrate==4.0.5: Flask-Migrate est une extension Flask pour gérer les migrations de base de données avec Alembic.

Flask-SQLAlchemy==3.1.1: Flask-SQLAlchemy est une extension Flask pour intégrer SQLAlchemy dans une application Flask.

Flask-WTF==1.2.1: Flask-WTF est une extension Flask pour gérer les formulaires web et la validation des données.

greenlet==3.0.3: Greenlet est une bibliothèque qui fournit des fils d'exécution légers pour Python.

idna==3.6: idna est une bibliothèque Python pour la normalisation et la comparaison des noms de domaine.

itsdangerous==2.1.2: itsdangerous est une bibliothèque pour gérer les objets signés en Python.

Jinja2==3.1.3: Jinja2 est un moteur de template pour Python, souvent utilisé avec Flask.

Mako==1.3.2: Mako est un moteur de template pour Python, similaire à Jinja2.

MarkupSafe==2.1.5: MarkupSafe est une bibliothèque Python pour la manipulation sécurisée de chaînes de caractères.

SQLAlchemy==2.0.25: SQLAlchemy est une bibliothèque Python pour l'interaction avec les bases de données relationnelles.

typing_extensions==4.9.0: typing_extensions fournit des extensions de typage pour Python.

Werkzeug==3.0.1: Werkzeug est une bibliothèque WSGI (Web Server Gateway Interface) pour Python, souvent utilisée avec Flask.

WTForms==3.1.2: WTForms est une bibliothèque de gestion de formulaires pour Flask.